SELECT
CASE WHEN (
SELECT COUNT() FROM MyNumbers
GROUP BY num
HAVING COUNT() = 1
) > 0
THEN ( SELECT num
FROM MyNumbers
GROUP BY num
HAVING COUNT(*) = 1
ORDER BY num DESC
LIMIT 1
)
ELSE NULL
END AS num;